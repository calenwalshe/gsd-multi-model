---
phase: 06-end-to-end-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/demo.sh
  - test/fixtures/demo-project/src/utils.js
  - test/fixtures/demo-project/package.json
  - test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02

must_haves:
  truths:
    - "Running bin/demo.sh executes the full loop: init-gsd bootstrap, plan creation, task splitting, worktree creation, codex-task execution (dry-run), worktree cleanup, and cross-review validation"
    - "Running bin/demo.sh validates each stage completed successfully (non-zero exit on failure) before advancing to the next stage"
    - "Running bin/demo.sh shows a final summary table with stage name, pass/fail status, duration, and artifacts produced"
    - "Running bin/demo.sh creates a temp sandbox in /tmp/gsd-demo-XXXX and auto-cleans on success, keeps on failure"
    - "Running bin/demo.sh --keep preserves the temp directory even on success"
    - "Running bin/demo.sh --live runs with real Codex execution instead of dry-run"
    - "Running bin/demo.sh --json outputs machine-readable JSON to stdout"
    - "Running bin/demo.sh performs pre-flight checks for git, node, and installed GSD skills before starting"
    - "Running bin/demo.sh aborts immediately on first stage failure with exit 1"
    - "Fixture project in test/fixtures/demo-project/ contains a minimal project with a PLAN.md that has XML task blocks with executor/confidence attributes"
  artifacts:
    - path: "bin/demo.sh"
      provides: "End-to-end demo script running full GSD dual-tool workflow in a temp sandbox"
      min_lines: 250
    - path: "test/fixtures/demo-project/src/utils.js"
      provides: "Minimal source file with a TODO for the demo task to implement"
      min_lines: 5
    - path: "test/fixtures/demo-project/package.json"
      provides: "Minimal package.json for stack detection"
      min_lines: 5
    - path: "test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md"
      provides: "Demo PLAN.md with XML task blocks for codex-task.sh to parse"
      min_lines: 40
  key_links:
    - from: "bin/demo.sh"
      to: "bin/worktree-create.sh"
      via: "creates worktree for parallel Codex execution stage"
      pattern: "bin/worktree-create.sh"
    - from: "bin/demo.sh"
      to: "bin/codex-task.sh"
      via: "executes Codex task in dry-run mode by default"
      pattern: "bin/codex-task.sh"
    - from: "bin/demo.sh"
      to: "bin/worktree-cleanup.sh"
      via: "cleans up worktree after Codex execution"
      pattern: "bin/worktree-cleanup.sh"
    - from: "bin/demo.sh"
      to: "test/fixtures/demo-project/"
      via: "copies fixture project into temp sandbox as demo target"
      pattern: "test/fixtures/demo-project"
---

<objective>
Create the end-to-end demo script and its fixture project. The demo proves the full dual-tool workflow loop runs without manual intervention: init-gsd bootstrap, plan with task routing, worktree creation, Codex execution (dry-run by default), merge-back, and cross-review validation.

Purpose: This is the capstone deliverable of the v1.1 milestone. It proves every component works together in sequence: installer, skills, worktrees, Codex wrapper, and cross-review.
Output: `bin/demo.sh` (executable demo script), `test/fixtures/demo-project/` (fixture files).
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-end-to-end-demo/06-CONTEXT.md
@bin/codex-task.sh (Codex wrapper — called during execution stage)
@bin/worktree-create.sh (worktree creation — called during worktree stage)
@bin/worktree-cleanup.sh (worktree cleanup — called during merge-back stage)
@install.sh (reference for ANSI output, pre-flight checks, shell patterns)
@skills/init-gsd/SKILL.md (reference for what init-gsd bootstrap produces)
</context>

<interfaces>
codex-task.sh interface:
  Usage: bin/codex-task.sh --plan PATH --task N --dry-run [--force] [--timeout N]
  JSON stdout: {"dry_run":true,"task_id":"...","executor":"...","confidence":"...","codex_command":"..."}
  Live JSON stdout: {"exit_code":0,"changed_files":[...],"commit_hash":"...","task_id":"...","duration_seconds":N,...}
  Exit codes: 0=success, 1=codex failure, 2=parse error, 3=timeout, 4=pre-flight

worktree-create.sh interface:
  Usage: bin/worktree-create.sh --task PLAN.md --json
  JSON: {"branch":"...","path":"...","base_commit":"...","base_ref":"..."}

worktree-cleanup.sh interface:
  Usage: bin/worktree-cleanup.sh BRANCH --json
  JSON: {"branch":"...","merge_commit":"...","files_changed":N,...}

init-gsd bootstrap creates:
  - AGENTS.md, CLAUDE.md, .claude/rules/, codex config
  - .planning/ directory with PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md
</interfaces>

<tasks>

<task type="auto" executor="claude" confidence="high">
  <name>Task 1: Create fixture project in test/fixtures/demo-project/</name>
  <files>test/fixtures/demo-project/src/utils.js, test/fixtures/demo-project/package.json, test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md</files>
  <action>
    Create a minimal fixture project that the demo script will copy into its temp sandbox. The fixture must be a valid target for the full GSD workflow: it needs source code, a package.json for stack detection, and a pre-built PLAN.md with XML task blocks that codex-task.sh can parse.

    **Create these files:**

    **1. `test/fixtures/demo-project/package.json`:**
    ```json
    {
      "name": "demo-project",
      "version": "1.0.0",
      "description": "GSD demo fixture project",
      "scripts": {
        "test": "node -e \"require('./src/utils.js'); console.log('ok')\""
      }
    }
    ```

    **2. `test/fixtures/demo-project/src/utils.js`:**
    ```javascript
    // Utility functions for demo project
    // TODO: Add isEmpty function that checks for null, undefined, empty string, empty array, empty object

    function capitalize(str) {
      if (typeof str !== 'string') return '';
      return str.charAt(0).toUpperCase() + str.slice(1);
    }

    module.exports = { capitalize };
    ```

    **3. `test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md`:**
    Create a valid PLAN.md that bin/codex-task.sh can parse. It needs:
    - Valid YAML frontmatter with phase, plan, wave, files_modified, requirements, autonomous, must_haves
    - An objective block
    - Two XML task blocks:
      - Task 1: executor="codex" confidence="high" — Add isEmpty utility function to src/utils.js
      - Task 2: executor="claude" confidence="high" — Review and verify the implementation (this one should NOT be dispatched to Codex)

    The PLAN.md frontmatter:
    ```yaml
    ---
    phase: 01-add-utils
    plan: 01
    type: execute
    wave: 1
    depends_on: []
    files_modified:
      - src/utils.js
    autonomous: true
    requirements:
      - UTIL-01

    must_haves:
      truths:
        - "src/utils.js exports an isEmpty function"
      artifacts:
        - path: "src/utils.js"
          provides: "isEmpty utility function"
          min_lines: 10
    ---
    ```

    The task blocks should follow the same XML format used throughout this project (see interfaces section above for the task tag format).

    Task 1 action should instruct: "Add an isEmpty function to src/utils.js that returns true for null, undefined, empty string (''), empty array ([]), and empty object ({}). Export it alongside the existing capitalize function."

    Task 1 done: "src/utils.js exports isEmpty. Running `node -e \"const {isEmpty} = require('./src/utils.js'); console.log(isEmpty(null), isEmpty(''), isEmpty([]), isEmpty({}))\"` prints 'true true true true'."

    Task 2 action should instruct: "Review the isEmpty implementation for edge cases. Verify it handles all specified types correctly."

    Task 2 done: "isEmpty handles null, undefined, empty string, empty array, empty object correctly."
  </action>
  <verify>
    <automated>test -f test/fixtures/demo-project/package.json && test -f test/fixtures/demo-project/src/utils.js && test -f "test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md" && python3 -c "import json; json.load(open('test/fixtures/demo-project/package.json'))" && echo "fixture ok" || echo "fixture missing or invalid"</automated>
  </verify>
  <done>test/fixtures/demo-project/ contains package.json (valid JSON), src/utils.js (with TODO), and .planning/phases/01-add-utils/01-01-PLAN.md (valid YAML frontmatter, 2 XML task blocks with executor/confidence attributes)</done>
</task>

<task type="auto" tdd="true" executor="claude" confidence="high">
  <name>Task 2: Create bin/demo.sh — end-to-end demo script</name>
  <files>bin/demo.sh</files>
  <action>
    Create `bin/demo.sh` — the end-to-end demo that proves the full dual-tool workflow loop. Follow the same shell conventions as `install.sh` and `bin/codex-task.sh` (set -euo pipefail, ANSI colors with TTY detection, ok/warn/err helpers on stderr).

    **CLI interface:**
    - `bin/demo.sh` — run full demo in dry-run mode (default)
    - `bin/demo.sh --live` — run with real Codex execution
    - `bin/demo.sh --keep` — preserve temp directory on success
    - `bin/demo.sh --json` — machine-readable JSON to stdout, human output to stderr

    **Script structure:**

    **Section 1 — Header and argument parsing:**
    - set -euo pipefail
    - ANSI color helpers matching existing bin/ patterns (TTY detection on stderr: `if [ -t 2 ]`)
    - Parse flags: --live, --keep, --json
    - Set defaults: DRY_RUN=true (unless --live), KEEP=false, JSON_OUTPUT=false
    - Determine GSD_ROOT: the repo root (where bin/demo.sh lives, go up one level from script dir)

    **Section 2 — Pre-flight checks:**
    Check and abort early if missing:
    1. `git` in PATH
    2. `node` in PATH
    3. GSD skills installed: check `~/.claude/skills/init-gsd/SKILL.md` exists
    4. Bin scripts exist: `$GSD_ROOT/bin/codex-task.sh`, `$GSD_ROOT/bin/worktree-create.sh`, `$GSD_ROOT/bin/worktree-cleanup.sh`
    5. Fixture project exists: `$GSD_ROOT/test/fixtures/demo-project/package.json`
    6. If --live: check `codex` in PATH
    Print each check with ok/err. Abort on first failure with exit 1.

    **Section 3 — Sandbox setup:**
    1. Create temp dir: `SANDBOX=$(mktemp -d /tmp/gsd-demo-XXXX)`
    2. Set up cleanup trap: `trap cleanup EXIT`
    3. cleanup() removes SANDBOX unless KEEP=true or any stage failed (for debugging)
    4. Copy fixture project into sandbox: `cp -r $GSD_ROOT/test/fixtures/demo-project/* $SANDBOX/`
    5. Initialize git in sandbox: `cd $SANDBOX && git init && git add -A && git commit -m "initial"`
    6. Print sandbox path to stderr

    **Section 4 — Stage execution engine:**
    Create a function `run_stage` that takes a stage name and a command:
    ```
    run_stage "Stage Name" command_function
    ```
    It should:
    - Print stage banner: `GSD > STAGE NAME` (matching ANSI patterns)
    - Record start time
    - Execute the command function
    - Record end time, calculate duration
    - On success: record pass in STAGES array with duration and artifacts
    - On failure: record fail, print error, abort immediately (stages are dependent per CONTEXT.md decision)

    Track stages in arrays:
    ```
    STAGE_NAMES=()
    STAGE_STATUS=()
    STAGE_DURATIONS=()
    STAGE_ARTIFACTS=()
    ```

    **Section 5 — Stage definitions:**
    Define each stage as a function. Each stage runs in the sandbox directory.

    **Stage 1: init-gsd bootstrap**
    Since init-gsd is a Claude Code skill (not a standalone script), simulate the bootstrap by creating the files it would produce:
    - Create AGENTS.md with basic project info
    - Create CLAUDE.md with project name
    - Create .claude/rules/ directory
    - The .planning/ directory with phases/01-add-utils/01-01-PLAN.md already exists from fixture copy
    Validate: AGENTS.md exists, CLAUDE.md exists, .planning/ exists
    Artifacts: "AGENTS.md, CLAUDE.md, .claude/rules/"

    **Stage 2: plan validation**
    Validate the pre-built PLAN.md from the fixture:
    - Check .planning/phases/01-add-utils/01-01-PLAN.md exists
    - Validate it has YAML frontmatter (grep for '---')
    - Validate it has XML task blocks (grep for '<task')
    - Count task blocks and report
    Artifacts: ".planning/phases/01-add-utils/01-01-PLAN.md (2 tasks)"

    **Stage 3: task splitting**
    Demonstrate task routing by reading executor attributes from the plan:
    - Parse task 1: expect executor="codex"
    - Parse task 2: expect executor="claude"
    - Report the split: "Task 1 → Codex (high confidence), Task 2 → Claude (high confidence)"
    Use codex-task.sh --dry-run to validate parsing works:
    - Run `$GSD_ROOT/bin/codex-task.sh --plan .planning/phases/01-add-utils/01-01-PLAN.md --task 1 --dry-run 2>/dev/null`
    - Validate JSON output has task_id and executor fields
    Artifacts: "2 tasks split: 1 codex, 1 claude"

    **Stage 4: worktree creation**
    Create a worktree for Codex execution:
    - Run `$GSD_ROOT/bin/worktree-create.sh --task .planning/phases/01-add-utils/01-01-PLAN.md --json`
    - Parse JSON to get branch and path
    - Validate worktree directory exists
    - Store WORKTREE_BRANCH and WORKTREE_PATH for later stages
    Artifacts: "worktree: $WORKTREE_BRANCH at $WORKTREE_PATH"

    **Stage 5: codex execution**
    Run codex-task.sh for the Codex-routed task:
    - If DRY_RUN: run `$GSD_ROOT/bin/codex-task.sh --plan .planning/phases/01-add-utils/01-01-PLAN.md --task 1 --dry-run`
    - If --live: run `$GSD_ROOT/bin/codex-task.sh --plan .planning/phases/01-add-utils/01-01-PLAN.md --task 1`
    - Validate exit code 0
    - Parse JSON output for task_id and (if live) changed_files/commit_hash
    Note: codex-task.sh creates its own worktree internally, so use the sandbox repo directly (don't cd into the worktree from stage 4 — stage 4 proves the worktree tooling works independently).
    Artifacts (dry-run): "codex-task dry-run: task 01-01-T1"
    Artifacts (live): "codex-task: $COMMIT_HASH, $N files changed"

    **Stage 6: worktree cleanup**
    Clean up the worktree created in stage 4:
    - Run `$GSD_ROOT/bin/worktree-cleanup.sh --no-merge --force $WORKTREE_BRANCH`
    - Validate exit code 0
    Artifacts: "worktree $WORKTREE_BRANCH removed"

    **Stage 7: cross-review validation**
    Validate that the workflow produced the expected artifacts:
    - AGENTS.md exists in sandbox
    - CLAUDE.md exists in sandbox
    - .planning/ directory exists
    - PLAN.md has XML task blocks
    - codex-task.sh produced valid JSON output (from stage 5)
    - If live: verify commit exists in git log
    This is structural validation (per CONTEXT.md decision) — not invoking both tools live.
    Artifacts: "7 checks passed"

    **Section 6 — Summary output:**
    Print a final summary table to stderr:
    ```
    ═══════════════════════════════════════════════════════
     GSD End-to-End Demo Complete
    ═══════════════════════════════════════════════════════
      Mode:     dry-run | live
      Sandbox:  /tmp/gsd-demo-XXXX
      Stages:   N/N passed

      Stage                   Status   Duration  Artifacts
      ─────                   ──────   ────────  ─────────
      init-gsd bootstrap      pass     0.1s      AGENTS.md, CLAUDE.md, ...
      plan validation         pass     0.0s      01-01-PLAN.md (2 tasks)
      task splitting          pass     0.2s      2 tasks: 1 codex, 1 claude
      worktree creation       pass     0.1s      worktree: gsd/demo-...
      codex execution         pass     0.3s      dry-run: task 01-01-T1
      worktree cleanup        pass     0.1s      worktree removed
      cross-review            pass     0.0s      7 checks passed
    ═══════════════════════════════════════════════════════
    ```

    If --json: output JSON to stdout with:
    ```json
    {
      "success": true,
      "mode": "dry-run",
      "sandbox": "/tmp/gsd-demo-XXXX",
      "stages": [
        {"name": "init-gsd bootstrap", "status": "pass", "duration_seconds": 0.1, "artifacts": ["AGENTS.md", "CLAUDE.md"]},
        ...
      ],
      "total_duration_seconds": 1.2
    }
    ```

    **Section 7 — Exit:**
    - Exit 0 if all stages passed
    - Exit 1 if any stage failed (should already have aborted, but as safety net)

    Make the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n bin/demo.sh && test -x bin/demo.sh && (bin/demo.sh 2>/dev/null; rc=$?; [ "$rc" -eq 0 ] && echo "demo ok" || echo "demo failed with exit $rc") 2>&1 | tail -5</automated>
  </verify>
  <done>bin/demo.sh exists, is executable, passes bash syntax check. Running bin/demo.sh in dry-run mode completes all 7 stages. Summary table shows all stages passed with durations and artifacts. Temp sandbox is cleaned up on success.</done>
</task>

</tasks>

<verification>
1. test/fixtures/demo-project/package.json is valid JSON
2. test/fixtures/demo-project/src/utils.js exists with TODO comment
3. test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md has valid frontmatter and 2 XML task blocks
4. bin/demo.sh passes `bash -n` syntax validation
5. bin/demo.sh is executable (`test -x`)
6. Running `bin/demo.sh` completes all 7 stages with exit 0
7. Running `bin/demo.sh` shows final summary table with pass status for all stages
8. Running `bin/demo.sh` creates and cleans up temp sandbox
9. bin/codex-task.sh --dry-run works against the fixture PLAN.md
</verification>

<success_criteria>
- bin/demo.sh runs the full workflow loop end-to-end in a temp sandbox without manual intervention
- Each stage validates its predecessor completed before starting
- Demo aborts on first stage failure with clear error identification
- Final summary shows all stages, durations, and artifacts
- Dry-run mode works without Codex CLI installed
- Fixture project provides a realistic target for the demo workflow
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-demo/06-01-SUMMARY.md`
</output>
