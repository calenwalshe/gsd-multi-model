---
phase: 06-end-to-end-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - test-demo.sh
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02

must_haves:
  truths:
    - "Running test-demo.sh validates bin/demo.sh completes all stages in dry-run mode"
    - "Running test-demo.sh validates pre-flight check failures (missing fixture, bad PATH)"
    - "Running test-demo.sh validates --keep flag preserves temp directory"
    - "Running test-demo.sh validates --json flag produces valid JSON to stdout"
    - "Running test-demo.sh validates sandbox cleanup on success"
    - "ROADMAP.md reflects Phase 6 plan details and marks Phase 5 complete"
    - "REQUIREMENTS.md marks DEMO-01 and DEMO-02 as complete"
  artifacts:
    - path: "test-demo.sh"
      provides: "Integration tests for bin/demo.sh covering all modes and failure cases"
      min_lines: 80
  key_links:
    - from: "test-demo.sh"
      to: "bin/demo.sh"
      via: "exercises dry-run, --keep, --json, and pre-flight checks"
      pattern: "bin/demo.sh"
---

<objective>
Create integration tests for bin/demo.sh and update project specs to reflect Phase 6 completion, closing out the v1.1 milestone.

Purpose: Validates the demo script handles all modes and edge cases, and that project documentation accurately reflects the final state.
Output: `test-demo.sh` (executable integration test), updated `ROADMAP.md` and `REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-end-to-end-demo/06-CONTEXT.md
@.planning/phases/06-end-to-end-demo/06-01-SUMMARY.md
@bin/demo.sh (the script being tested)
@test-install.sh (reference for test script style)
@test-codex-task.sh (reference for test patterns with temp fixtures)
</context>

<tasks>

<task type="auto" tdd="true" executor="codex" confidence="high">
  <name>Task 1: Create test-demo.sh — integration tests for bin/demo.sh</name>
  <files>test-demo.sh</files>
  <behavior>
    - Test: bin/demo.sh runs all stages in dry-run mode → exit 0
    - Test: bin/demo.sh --json produces valid JSON to stdout with success=true and stages array
    - Test: bin/demo.sh --keep preserves temp directory after success
    - Test: bin/demo.sh cleans up temp directory on success (default behavior)
    - Test: fixture project exists and has required files
    - Test: summary output contains "GSD End-to-End Demo Complete"
    - Test: all tests produce pass/fail summary at end
  </behavior>
  <action>
    Create `test-demo.sh` — an integration test that validates bin/demo.sh modes and behaviors. Follow the test style from `test-install.sh` and `test-codex-task.sh` (ok/err helpers, pass/fail counts, summary at end).

    **Test structure:**
    ```
    PASS=0 FAIL=0
    ok()   { ... PASS++ }
    err()  { ... FAIL++ }
    ```

    **Test cases:**

    1. **Fixture validation**
       - Assert test/fixtures/demo-project/package.json exists
       - Assert test/fixtures/demo-project/src/utils.js exists
       - Assert test/fixtures/demo-project/.planning/phases/01-add-utils/01-01-PLAN.md exists

    2. **Dry-run full execution**
       - Run `bin/demo.sh 2>/tmp/gsd-test-demo-stderr-$$.log`
       - Assert exit code 0
       - Assert stderr contains "GSD End-to-End Demo Complete" (from the log file)

    3. **JSON output mode**
       - Run `JSON_OUT=$(bin/demo.sh --json 2>/dev/null)`
       - Assert exit code 0
       - Parse JSON: `echo "$JSON_OUT" | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['success']==True; assert len(d['stages'])>=6; print('json ok')"`

    4. **--keep flag preserves sandbox**
       - Run `SANDBOX_PATH=$(bin/demo.sh --json --keep 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin)['sandbox'])")`
       - Assert the sandbox directory still exists
       - Clean it up manually after test: `rm -rf "$SANDBOX_PATH"`

    5. **Default cleanup**
       - Run `SANDBOX_PATH=$(bin/demo.sh --json 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin)['sandbox'])")`
       - Assert the sandbox directory does NOT exist (was cleaned up)

    6. **Stage count**
       - From the JSON output of test 3, verify at least 6 stages present
       - Verify all stages have status "pass"

    **Summary output:**
    ```
    ═══════════════════════════════════════════════════════
     Demo Tests: N passed, M failed
    ═══════════════════════════════════════════════════════
    ```

    Exit 0 if all pass, exit 1 if any fail.

    Clean up all temp files in a trap.

    Make the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n test-demo.sh && test -x test-demo.sh && bash test-demo.sh; rc=$?; [ "$rc" -eq 0 ] && echo "all tests passed" || echo "test failures (exit $rc)"</automated>
  </verify>
  <done>test-demo.sh exists, is executable, passes bash syntax check, all test cases pass. Tests validate: dry-run execution, JSON output, --keep flag, sandbox cleanup, stage completeness.</done>
</task>

<task type="auto" executor="claude" confidence="high">
  <name>Task 2: Update ROADMAP.md and REQUIREMENTS.md for Phase 6 completion</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    Update project specs to reflect Phase 6 plan creation and (after execution) completion of the v1.1 milestone.

    **ROADMAP.md updates:**
    1. Find Phase 6 section (`### Phase 6: End-to-End Demo`)
    2. Update `**Plans**: TBD` to `**Plans**: 2 plans`
    3. Replace the placeholder plan list with:
       ```
       Plans:
       - [ ] 06-01-PLAN.md — Create bin/demo.sh and test/fixtures/demo-project/ fixture
       - [ ] 06-02-PLAN.md — Integration tests for demo.sh and spec updates
       ```
    4. Mark Phase 5 plans as complete (replace `- [ ]` with `- [x]` for 05-01 and 05-02)
    5. Update the progress table: Phase 5 status to "Complete" with date, Phase 6 to "In Progress"

    **REQUIREMENTS.md updates:**
    1. Find the End-to-End Demo section
    2. Keep DEMO-01 and DEMO-02 as `- [ ]` (they will be marked complete after execution, not during planning)
    3. Verify traceability table has correct Phase 6 mapping for DEMO-01 and DEMO-02
  </action>
  <verify>
    <automated>grep -q "06-01-PLAN.md" .planning/ROADMAP.md && grep -q "06-02-PLAN.md" .planning/ROADMAP.md && grep -q "DEMO-01.*Phase 6" .planning/REQUIREMENTS.md && echo "specs updated" || echo "specs missing entries"</automated>
  </verify>
  <done>ROADMAP.md lists both Phase 6 plans with descriptions. Phase 5 entries marked complete. REQUIREMENTS.md has DEMO-01 and DEMO-02 mapped to Phase 6 in traceability table.</done>
</task>

</tasks>

<verification>
1. test-demo.sh passes `bash -n` syntax validation
2. test-demo.sh is executable
3. Running `bash test-demo.sh` from repo root completes with all tests passing
4. ROADMAP.md contains entries for 06-01-PLAN.md and 06-02-PLAN.md
5. ROADMAP.md Phase 5 entries are marked [x] (complete)
6. REQUIREMENTS.md has DEMO-01 and DEMO-02 in traceability table mapped to Phase 6
</verification>

<success_criteria>
- test-demo.sh validates all critical paths of bin/demo.sh without requiring Codex CLI installed
- Tests cover: dry-run execution, JSON output, --keep flag, sandbox cleanup, fixture validation
- ROADMAP.md and REQUIREMENTS.md accurately reflect Phase 6 plan structure
- All tests pass when run from repo root
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-demo/06-02-SUMMARY.md`
</output>
