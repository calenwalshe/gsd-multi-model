---
phase: 05-codex-execution-wrapper
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - test-codex-task.sh
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CODEX-01
  - CODEX-02
  - CODEX-03

must_haves:
  truths:
    - "Running test-codex-task.sh validates XML parsing against real PLAN.md files"
    - "Running test-codex-task.sh validates pre-flight exit codes (no args=4, bad plan=2, bad task=2)"
    - "Running test-codex-task.sh validates --dry-run produces valid JSON with all required fields"
    - "Running test-codex-task.sh validates executor/confidence routing logic"
    - "ROADMAP.md reflects the actual plans created for Phase 5"
  artifacts:
    - path: "test-codex-task.sh"
      provides: "Integration test for codex-task.sh covering parsing, pre-flight, dry-run, and routing"
      min_lines: 80
  key_links:
    - from: "test-codex-task.sh"
      to: "bin/codex-task.sh"
      via: "exercises all exit codes and --dry-run JSON output"
      pattern: "bin/codex-task.sh"
---

<objective>
Create integration tests for codex-task.sh and update project specs to reflect the completed phase.

Purpose: Validates that the Codex wrapper handles all edge cases correctly and that project documentation stays current. The test script exercises parsing, pre-flight checks, dry-run, and routing logic without requiring Codex CLI installed.
Output: `test-codex-task.sh` (executable integration test), updated `ROADMAP.md`.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codex-execution-wrapper/05-CONTEXT.md
@.planning/phases/05-codex-execution-wrapper/05-01-SUMMARY.md
@bin/codex-task.sh (the script being tested)
@test-install.sh (reference for test script style: ok/err helpers, summary)
@test-worktree.sh (reference for test patterns: temp git repos, lifecycle tests)
</context>

<tasks>

<task type="auto" tdd="true" executor="codex" confidence="high">
  <name>Task 1: Create test-codex-task.sh — integration tests for codex-task.sh</name>
  <files>test-codex-task.sh</files>
  <behavior>
    - Test: no arguments → exit 4
    - Test: nonexistent plan file → exit 2
    - Test: nonexistent task number → exit 2
    - Test: --dry-run with real PLAN.md → exit 0, valid JSON output with task_id, executor, confidence, dry_run=true
    - Test: --dry-run extracts correct task by number (task 1 vs task 2)
    - Test: task with executor="claude" without --force → exit 4
    - Test: task with executor="claude" with --force --dry-run → exit 0 (proceeds)
    - Test: confidence routing: high → full-auto in command, medium → no flag, low → exit 4
    - Test: all tests produce pass/fail summary at end
  </behavior>
  <action>
    Create `test-codex-task.sh` — an integration test that validates bin/codex-task.sh parsing, pre-flight checks, and dry-run mode. Follow the test style from `test-install.sh` and `test-worktree.sh` (ok/err helpers, pass/fail counts, summary at end).

    **Key design decision:** All tests use `--dry-run` mode so they do NOT require Codex CLI to be installed. This tests the parsing and routing logic without external dependencies.

    **Test structure:**

    ```
    PASS=0 FAIL=0
    ok()   { ... PASS++ }
    err()  { ... FAIL++ }
    ```

    **Test cases:**

    1. **Pre-flight: no arguments**
       - Run `bin/codex-task.sh 2>&1; rc=$?`
       - Assert rc == 4
       - Assert stderr contains "usage" or "--plan"

    2. **Pre-flight: nonexistent plan**
       - Run `bin/codex-task.sh --plan /tmp/nonexistent-$$.md --task 1 2>&1; rc=$?`
       - Assert rc == 2

    3. **Pre-flight: nonexistent task number**
       - Run `bin/codex-task.sh --plan .planning/phases/04-worktree-automation/04-01-PLAN.md --task 99 2>&1; rc=$?`
       - Assert rc == 2

    4. **Dry-run: basic parsing (task 1)**
       - Run `bin/codex-task.sh --plan .planning/phases/04-worktree-automation/04-01-PLAN.md --task 1 --dry-run 2>/dev/null`
       - Parse JSON output
       - Assert task_id == "04-01-T1"
       - Assert executor == "codex"
       - Assert confidence == "high"
       - Assert dry_run == true

    5. **Dry-run: task selection (task 2)**
       - Run `bin/codex-task.sh --plan .planning/phases/04-worktree-automation/04-01-PLAN.md --task 2 --dry-run 2>/dev/null`
       - Parse JSON output
       - Assert task_id == "04-01-T2"

    6. **Dry-run: multi-line action extraction**
       - From test 4 JSON output, verify `codex_command` field is non-empty (action was extracted)

    7. **Executor validation: claude task without --force**
       - Create a minimal temporary PLAN.md with a single task block. The task should have executor="claude" and confidence="high", with name "Task 1: Test task", files "test.txt", action "Do something", and done "Done". Include valid YAML frontmatter (phase: test, plan: 01, files_modified: []).
       - Run `bin/codex-task.sh --plan $TEMP_PLAN --task 1 --dry-run 2>&1; rc=$?`
       - Assert rc == 4 (executor mismatch)

    8. **Executor validation: claude task with --force**
       - Run `bin/codex-task.sh --plan $TEMP_PLAN --task 1 --dry-run --force 2>/dev/null; rc=$?`
       - Assert rc == 0 (force override)

    9. **Confidence routing: low confidence**
       - Create temp PLAN.md with `confidence="low"`
       - Run `bin/codex-task.sh --plan $TEMP_PLAN --task 1 --dry-run 2>&1; rc=$?`
       - Assert rc == 4 (low confidence rejection)

    10. **Confidence routing: high vs medium**
        - From test 4 JSON, verify codex_command contains "--full-auto" (high confidence)
        - Create temp PLAN.md with `confidence="medium"`, run dry-run
        - Verify codex_command does NOT contain "--full-auto"

    **Summary output:**
    ```
    ═══════════════════════════════════════════════════════
     Codex Task Tests: N passed, M failed
    ═══════════════════════════════════════════════════════
    ```

    Exit 0 if all pass, exit 1 if any fail.

    Clean up all temp files in a trap.

    Make the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n test-codex-task.sh && test -x test-codex-task.sh && bash test-codex-task.sh; rc=$?; [ "$rc" -eq 0 ] && echo "all tests passed" || echo "test failures (exit $rc)"</automated>
  </verify>
  <done>test-codex-task.sh exists, is executable, passes bash syntax check, all test cases pass. Tests validate: pre-flight exit codes, XML task parsing, dry-run JSON output, executor validation with/without --force, confidence routing (high/medium/low).</done>
</task>

<task type="auto" executor="claude" confidence="high">
  <name>Task 2: Update ROADMAP.md with Phase 5 plan details</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update `.planning/ROADMAP.md` to reflect the plans created for Phase 5.

    1. Read current ROADMAP.md
    2. Find the Phase 5 section (`### Phase 5: Codex Execution Wrapper`)
    3. Update `**Plans**: TBD` → `**Plans**: 2 plans`
    4. Replace the placeholder plan list:
       ```
       Plans:
       - [ ] 05-01: TBD
       ```
       With:
       ```
       Plans:
       - [ ] 05-01-PLAN.md — Create bin/codex-task.sh with XML parsing, context injection, Codex invocation, structured output
       - [ ] 05-02-PLAN.md — Integration tests for codex-task.sh and ROADMAP update
       ```
    5. Update the Phase 4 plans in ROADMAP to reflect completed status (replace `- [ ]` with `- [x]` for 04-01 and 04-02)
    6. Update the progress table: Phase 4 status to "Complete", Phase 5 status to "In Progress"
  </action>
  <verify>
    <automated>grep -q "05-01-PLAN.md" .planning/ROADMAP.md && grep -q "05-02-PLAN.md" .planning/ROADMAP.md && echo "roadmap updated" || echo "roadmap missing plan entries"</automated>
  </verify>
  <done>ROADMAP.md lists both Phase 5 plans with descriptive objectives, Phase 4 entries marked complete, progress table updated</done>
</task>

</tasks>

<verification>
1. test-codex-task.sh passes `bash -n` syntax validation
2. test-codex-task.sh is executable
3. Running `bash test-codex-task.sh` from repo root completes with all tests passing
4. ROADMAP.md contains entries for 05-01-PLAN.md and 05-02-PLAN.md
5. ROADMAP.md Phase 4 entries are marked [x] (complete)
</verification>

<success_criteria>
- test-codex-task.sh validates all critical paths of codex-task.sh without requiring Codex CLI installed
- Tests cover: pre-flight errors, XML parsing, dry-run JSON, executor validation, confidence routing
- ROADMAP.md accurately reflects Phase 5 plan structure
- All tests pass when run from repo root
</success_criteria>

<output>
After completion, create `.planning/phases/05-codex-execution-wrapper/05-02-SUMMARY.md`
</output>
