---
phase: 03-installer-hardening-global-config
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - test-install.sh
autonomous: true
requirements:
  - INST-02

must_haves:
  truths:
    - "Running test-install.sh after a clean install reports all files as matching their sources"
    - "Running test-install.sh after manually modifying an installed file detects the mismatch"
    - "test-install.sh reports diff-based integrity results alongside existence checks"
  artifacts:
    - path: "test-install.sh"
      provides: "Diff-based integrity validation for installed files"
      contains: "check_integrity"
  key_links:
    - from: "test-install.sh"
      to: "install.sh"
      via: "Validates the same file pairs install.sh copies"
      pattern: "cmp.*skills\\|cmp.*global"
---

<objective>
Upgrade test-install.sh to perform diff-based integrity checks alongside existing existence checks.

Purpose: test-install.sh becomes a standalone verification tool that can detect corrupted or outdated installations, not just missing files.
Output: Enhanced test-install.sh with both existence and content-match validation.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer-hardening-global-config/03-CONTEXT.md
@.planning/phases/03-installer-hardening-global-config/03-01-SUMMARY.md

@test-install.sh
@install.sh

<interfaces>
<!-- Current test-install.sh (51 lines):
  - check() function: tests -e, prints ✓/✗, counts PASS/FAIL
  - Checks: skills (SKILL.md), GSD commands, global configs, GSD agents
  - Exit 0 on all pass, exit 1 on any fail

  Enhancement needed:
  - New check_integrity() function: uses cmp -s to compare source vs installed
  - Skills = direct copies → strict match expected
  - Configs = templates → warn on mismatch (user may have customized)
  - Must detect SCRIPT_DIR to locate source files for comparison
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diff-based integrity checks to test-install.sh</name>
  <files>test-install.sh</files>
  <action>
Enhance test-install.sh with content-match validation:

1. **Add SCRIPT_DIR detection** (after set -euo pipefail):
   - `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
   - This is needed to locate source files for comparison

2. **Add WARN counter** alongside existing PASS/FAIL:
   - `WARN=0`

3. **Add check_integrity() function** after existing check():
   - Parameters: `$1` = source file, `$2` = installed destination, `$3` = "strict" or "template"
   - If dest does not exist: call existing `check "$2"` pattern (counts as FAIL) and return
   - If `cmp -s "$1" "$2"`: print `"  ✓ $2 matches source"`, increment PASS
   - If mismatch AND mode is "strict": print `"  ✗ $2 DIFFERS from source (re-run install.sh)"`, increment FAIL
   - If mismatch AND mode is "template": print `"  ⚠ $2 differs from template (user-customized?)"`, increment WARN

4. **Add integrity check sections** after existing check sections:
   - New section "=== Checking skill integrity ===" — for each skill dir in `$SCRIPT_DIR/skills/*/`:
     - For each file in the skill dir, `check_integrity "$src_file" "$HOME/.claude/skills/$skill_name/$(basename $file)" "strict"`
   - New section "=== Checking rules integrity ===" — for each rule in `$SCRIPT_DIR/rules/*.md`:
     - `check_integrity "$rule" "$HOME/.claude/rules/$(basename $rule)" "template"`
   - New section "=== Checking config integrity ===" :
     - `check_integrity "$SCRIPT_DIR/global/codex-agents.md" "$HOME/.codex/AGENTS.md" "template"`
     - `check_integrity "$SCRIPT_DIR/global/codex-config.toml" "$HOME/.codex/config.toml" "template"`

5. **Update summary** to include WARN count:
   - `echo " Results: $PASS passed, $FAIL failed, $WARN warnings"`

6. **Keep all existing existence checks unchanged** — integrity checks are additive, not replacing.
  </action>
  <verify>
    <automated>cd /home/agent/gsd-multi-model && bash -n test-install.sh && echo "Syntax OK"</automated>
  </verify>
  <done>test-install.sh has check_integrity() function using cmp -s, integrity sections for skills (strict), rules (template), and configs (template), warn counter in summary, all existing existence checks preserved.</done>
</task>

</tasks>

<verification>
1. `bash -n test-install.sh` — syntax validation passes
2. `grep -c 'check_integrity\|cmp' test-install.sh` — integrity function and cmp usage present
3. `grep 'strict\|template' test-install.sh` — both modes used appropriately
4. `grep 'WARN' test-install.sh` — warning counter exists
</verification>

<success_criteria>
- test-install.sh checks both file existence AND content match against source
- Skills use strict mode (mismatch = FAIL)
- Configs and rules use template mode (mismatch = WARN, not fail)
- Summary shows pass/fail/warn counts
- All existing existence checks still present and working
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer-hardening-global-config/03-02-SUMMARY.md`
</output>
