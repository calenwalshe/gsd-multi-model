---
phase: 03-installer-hardening-global-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "ROADMAP SC1 says 'git or node' (not 'Claude Code or git') as the deps that produce hard errors"
    - "ROADMAP SC4 says 'conservative defaults with approval policy' (not '--full-auto defaults')"
    - "INST-01 lists claude as optional, git and node as required"
    - "CONF-02 describes conservative approval defaults, not --full-auto"
    - "All 6 requirement IDs remain checked as complete"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated success criteria SC1 and SC4"
      contains: "git or node"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated INST-01 and CONF-02 descriptions"
      contains: "optional"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "install.sh"
      via: "SC1 and SC4 now match actual install.sh behavior"
      pattern: "git or node"
    - from: ".planning/REQUIREMENTS.md"
      to: "install.sh"
      via: "INST-01 and CONF-02 descriptions match implemented behavior"
      pattern: "optional"
---

<objective>
Update ROADMAP.md and REQUIREMENTS.md spec text to align with implemented behavior, closing two verification gaps.

Purpose: The code is correct (install.sh treats claude as optional, codex config uses conservative defaults). The specs were not updated to match the discuss-phase decisions. This plan fixes the specs.
Output: Updated ROADMAP.md and REQUIREMENTS.md with accurate descriptions.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-installer-hardening-global-config/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md success criteria to match implementation</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit the Phase 3 success criteria in ROADMAP.md:

1. SC1 (line ~38): Change from "Running `install.sh` on a machine missing Claude Code or git produces a clear error naming the missing dependency and how to install it" to "Running `install.sh` on a machine missing git or node produces a clear error naming the missing dependency and how to install it"

2. SC4 (line ~41): Change from "Running `install.sh` installs global Codex config with `--full-auto` defaults without overwriting existing user config" to "Running `install.sh` installs global Codex config with conservative approval defaults without overwriting existing user config"

3. Update the Phase 3 plan list to include this gap closure plan:
   Add "- [ ] 03-03-PLAN.md -- Update specs (ROADMAP, REQUIREMENTS) to match implemented behavior"

4. Update the Plans count from "2 plans" to "3 plans"

Do NOT change any other phases or sections.
  </action>
  <verify>
    <automated>grep -q "missing git or node" .planning/ROADMAP.md && grep -q "conservative approval defaults" .planning/ROADMAP.md && grep -q "03-03-PLAN.md" .planning/ROADMAP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ROADMAP SC1 references "git or node" (not "Claude Code or git"), SC4 references "conservative approval defaults" (not "--full-auto defaults"), plan list includes 03-03</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md descriptions to match implementation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit the Phase 3 requirements in REQUIREMENTS.md:

1. INST-01 (line ~29): Change from "install.sh checks for required dependencies (Claude Code, git, node) before installing" to "install.sh checks for required dependencies (git, node) and optional dependencies (claude, codex) before installing"

2. CONF-02 (line ~36): Change from "Global Codex config template installed with `--full-auto` and approval settings" to "Global Codex config template installed with conservative approval defaults (untrusted policy)"

Both requirements should remain checked as complete ([x]).

Do NOT change any other requirements or sections.
  </action>
  <verify>
    <automated>grep -q "optional dependencies (claude, codex)" .planning/REQUIREMENTS.md && grep -q "conservative approval defaults" .planning/REQUIREMENTS.md && grep -c "\[x\].*INST-01\|CONF-02" .planning/REQUIREMENTS.md | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>INST-01 lists claude as optional (not required), CONF-02 describes conservative defaults (not --full-auto), both remain marked complete</done>
</task>

</tasks>

<verification>
After both tasks:
1. ROADMAP SC1 mentions "git or node" not "Claude Code or git"
2. ROADMAP SC4 mentions "conservative approval defaults" not "--full-auto defaults"
3. INST-01 lists claude/codex as optional deps
4. CONF-02 describes conservative approval defaults
5. All 6 requirement IDs (INST-01 through CONF-03) remain [x] checked
6. No code files (install.sh, test-install.sh) were modified
</verification>

<success_criteria>
Spec documents (ROADMAP.md, REQUIREMENTS.md) accurately describe what install.sh actually does. Both verification gaps from 03-VERIFICATION.md are resolved by spec alignment. Re-verification of Phase 3 would score 10/10.
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer-hardening-global-config/03-03-SUMMARY.md`
</output>
