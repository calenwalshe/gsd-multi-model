---
phase: 03-installer-hardening-global-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "Running install.sh on a machine missing git or node produces a clear error naming the missing dep and how to install it"
    - "Running install.sh on a machine missing claude or codex CLI prints a warning (not error) and continues"
    - "Running install.sh with --force overwrites all config files with fresh templates"
    - "Running install.sh without --force skips existing config files with a message"
    - "After install completes, a summary shows counts of installed, skipped, warnings, and errors"
    - "After install completes, an integrity check compares each installed file against its source"
    - "ANSI colors are used for output (red=error, green=success, yellow=warning) with TTY fallback"
  artifacts:
    - path: "install.sh"
      provides: "Hardened installer with pre-flight checks, integrity validation, ANSI output, --force flag, unified config strategy"
      contains: "preflight_check"
  key_links:
    - from: "install.sh"
      to: "global/codex-agents.md"
      via: "cp for config template installation"
      pattern: "cp.*codex-agents"
    - from: "install.sh"
      to: "global/codex-config.toml"
      via: "cp for config template installation"
      pattern: "cp.*codex-config"
    - from: "install.sh"
      to: "skills/"
      via: "cp -r for skill installation"
      pattern: "cp -r.*skills"
---

<objective>
Harden install.sh with dependency pre-flight checks, post-install integrity validation, ANSI colored output, a --force flag, and unified skip-if-exists config strategy.

Purpose: Users get reliable installation with clear feedback — errors name the missing tool and how to install it, warnings for optional deps, and integrity verification confirms files were copied correctly.
Output: Rewritten install.sh with all 6 Phase 3 requirements addressed.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer-hardening-global-config/03-CONTEXT.md

@install.sh
@global/codex-agents.md
@global/codex-config.toml
@rules/gsd-workflow.md

<interfaces>
<!-- Current install.sh structure (226 lines, 7 sections):
  Section 1: Install skills into ~/.claude/skills/
  Section 2: Install .claude/rules/ templates
  Section 3: Set up global Claude preferences (CLAUDE.md) — uses grep-and-append
  Section 4: Set up global Codex config (AGENTS.md, config.toml) — mixed strategies
  Section 5: Install GSD framework across all runtimes
  Section 6: Create .gitignore
  Section 7: Summary

  Patterns:
  - Section headers: "==> Section name..."
  - Banners: ═══ double-line box characters
  - Items: 4-space indented
  - set -euo pipefail at top
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-flight checks, --force flag, and ANSI output helpers</name>
  <files>install.sh</files>
  <action>
Modify install.sh to add the following BEFORE any installation logic (before section 1):

1. **ANSI color helpers with TTY detection** (after set -euo pipefail, before banner):
   - Detect TTY: `if [ -t 1 ]; then` set RED='\033[0;31m', GREEN='\033[0;32m', YELLOW='\033[1;33m', BOLD='\033[1m', RESET='\033[0m'; else set all to empty string
   - Helper functions: `ok() { echo -e "${GREEN}  ✓${RESET} $1"; }`, `warn() { echo -e "${YELLOW}  ⚠${RESET} $1"; WARNINGS=$((WARNINGS + 1)); }`, `err() { echo -e "${RED}  ✗${RESET} $1"; ERRORS=$((ERRORS + 1)); }`
   - Counter variables at top: `INSTALLED=0 SKIPPED=0 WARNINGS=0 ERRORS=0`

2. **Parse --force flag** (after SCRIPT_DIR, before banner):
   - `FORCE=false; for arg in "$@"; do [ "$arg" = "--force" ] && FORCE=true; done`

3. **Pre-flight dependency check function** (after banner):
   - Function `preflight_check()` that checks ALL deps first, collects failures, then reports summary:
     - Required deps (hard fail): `git`, `node` — check with `command -v`
     - Optional deps (warn only): `claude`, `codex` — check with `command -v`
   - For each missing required dep, add to a MISSING_REQUIRED array with platform-specific install hint:
     - git: "brew install git (macOS) / apt install git (Linux)"
     - node: "brew install node (macOS) / apt install nodejs (Linux) / https://nodejs.org"
   - For each missing optional dep, call `warn "X not found. Install: Y"`
     - claude: "https://docs.anthropic.com/en/docs/claude-code"
     - codex: "npm install -g @openai/codex"
   - Platform detection: `case "$(uname -s)" in Darwin*) PLATFORM="macOS";; Linux*) PLATFORM="Linux";; *) PLATFORM="unknown";; esac`
   - After checking all deps: if MISSING_REQUIRED is non-empty, print all errors with install commands, then `exit 1`
   - Call `preflight_check` immediately after defining it (before section 1)

4. **Update existing config sections (3 and 4) to unified skip-if-exists**:
   - Section 3 (Claude CLAUDE.md): REMOVE the grep-and-append branch entirely. Logic becomes:
     - If `--force`: overwrite with template, call `ok`
     - If file exists: call `ok "~/.claude/CLAUDE.md exists, skipping"`, increment SKIPPED
     - If file missing: create from template (keep existing heredoc content), call `ok`, increment INSTALLED
   - Section 4 (Codex AGENTS.md): Same pattern — remove the grep-and-update branch:
     - If `--force`: `cp` from template, call `ok`
     - If file exists: skip with message, increment SKIPPED
     - If file missing: `cp` from template, call `ok`, increment INSTALLED
   - Section 4 (Codex config.toml): Already skip-if-exists, just add --force support and use `ok`/`warn` helpers

5. **Update all existing echo statements** to use the `ok`/`warn`/`err` helpers where appropriate:
   - Skill installed/updated → `ok`
   - Config skipped → `ok` with "exists, skipping" message (increment SKIPPED)
   - Config created → `ok` (increment INSTALLED)
   - Keep section headers ("==> ...") as plain echo (these are structural, not status)

6. **Keep existing banner style** (═══) unchanged. Keep section structure and numbering unchanged.
  </action>
  <verify>
    <automated>cd /home/agent/gsd-multi-model && bash -n install.sh && echo "Syntax OK"</automated>
  </verify>
  <done>install.sh has pre-flight checks (git, node required; claude, codex optional), --force flag, ANSI color helpers with TTY detection, unified skip-if-exists for all configs, and all output uses ok/warn/err helpers. Syntax check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add post-install integrity validation and end summary</name>
  <files>install.sh</files>
  <action>
Add integrity validation and updated summary to install.sh:

1. **Integrity validation function** (define near top with other functions, call after section 6 / before section 7 summary):
   - Function `verify_integrity()` that compares installed files against their sources using `cmp -s`:
   - Build an array of source→dest pairs from the installation sections:
     - Skills: for each skill_dir in skills/*/, compare "$SCRIPT_DIR/skills/$skill_name/" files against "$HOME/.claude/skills/$skill_name/"
     - Rules: for each rule in rules/*.md, compare source against "$HOME/.claude/rules/$rule_name"
     - Codex AGENTS.md: compare "$SCRIPT_DIR/global/codex-agents.md" against "$HOME/.codex/AGENTS.md"
     - Codex config.toml: compare "$SCRIPT_DIR/global/codex-config.toml" against "$HOME/.codex/config.toml"
   - For skills (direct copies): if `cmp -s` fails, call `err "Mismatch: $dest differs from source $src"` — these MUST match
   - For configs (templates): if `cmp -s` fails AND file was NOT skipped (i.e., was freshly installed or --force), call `err`; if file was skipped, that is expected and should not be flagged
   - Track which files were skipped during installation (use a SKIPPED_FILES array populated during sections 2-4) so integrity check can distinguish "skipped by user choice" from "copy failed"
   - Print integrity section header: `echo ""; echo "==> Verifying installation integrity..."`

2. **Update section 7 summary** to show counters:
   - After the existing summary content, add a status line:
     `echo " Status: $INSTALLED installed, $SKIPPED skipped, $WARNINGS warnings, $ERRORS errors"`
   - If ERRORS > 0: add `echo -e "${RED} Some files failed integrity checks. Re-run with --force to reinstall.${RESET}"` and `exit 1`
   - If WARNINGS > 0 and ERRORS = 0: exit 0 (warnings are informational)

3. **Add --force documentation** to the usage header comment at top of file:
   - Update the Usage section to include: `bash install.sh --force   # Overwrite existing configs`
  </action>
  <verify>
    <automated>cd /home/agent/gsd-multi-model && bash -n install.sh && echo "Syntax OK"</automated>
  </verify>
  <done>install.sh has post-install integrity validation comparing installed files against sources, end summary with install/skip/warn/error counts, exit 1 on integrity errors, --force documented in usage header.</done>
</task>

</tasks>

<verification>
1. `bash -n install.sh` — syntax validation passes
2. `grep -c 'preflight_check\|verify_integrity\|FORCE\|command -v' install.sh` — all key functions/patterns present
3. `grep 'RED\|GREEN\|YELLOW' install.sh` — ANSI colors defined
4. `grep -c 'grep.*CLAUDE_GLOBAL\|grep.*CODEX_AGENTS' install.sh` should be 0 — grep-and-append removed
</verification>

<success_criteria>
- install.sh checks git and node before installing (hard fail with install hints if missing)
- install.sh warns about missing claude/codex CLI (soft warning, continues)
- install.sh --force overwrites all config files
- install.sh without --force skips existing configs uniformly (no grep-and-append)
- install.sh verifies installed file integrity via cmp after copying
- install.sh prints colored output (red/green/yellow) with TTY fallback
- install.sh prints end summary with installed/skipped/warnings/errors counts
- install.sh exits 1 on pre-flight failure or integrity errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer-hardening-global-config/03-01-SUMMARY.md`
</output>
