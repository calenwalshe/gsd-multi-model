---
phase: 01-the-orchestrator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - global/workflows/discuss-phase.md
  - global/workflows/plan-phase.md
  - global/workflows/execute-phase.md
  - global/workflows/transition.md
autonomous: true
requirements: [ORCH-01]

must_haves:
  truths:
    - "No workflow file contains --auto flag handling or auto_advance steps"
    - "Manual invocation of each skill still works without --auto"
    - "transition.md yolo mode routes through /gsd:drive instead of --auto chaining"
  artifacts:
    - path: "global/workflows/discuss-phase.md"
      provides: "Discuss phase without --auto sections"
    - path: "global/workflows/plan-phase.md"
      provides: "Plan phase without --auto sections"
    - path: "global/workflows/execute-phase.md"
      provides: "Execute phase without --auto sections"
    - path: "global/workflows/transition.md"
      provides: "Transition without --auto chaining, yolo routes to /gsd:drive"
  key_links:
    - from: "global/workflows/transition.md"
      to: "/gsd:drive"
      via: "yolo mode recommendation"
      pattern: "gsd:drive"
---

<objective>
Remove the --auto flag mechanism from all existing workflow files. /gsd:drive replaces --auto as the single entry point for chaining.

Purpose: Hard cut per locked decision -- anyone using --auto should switch to /gsd:drive. v2.0 is a major version bump so breaking changes are expected.
Output: Updated discuss-phase.md, plan-phase.md, execute-phase.md, transition.md with --auto sections removed.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-the-orchestrator/01-CONTEXT.md
@.planning/phases/01-the-orchestrator/01-RESEARCH.md

<interfaces>
<!-- The --auto sections to remove are found at these locations -->

discuss-phase.md:
- Line ~589: `<step name="auto_advance">` section
- Line ~592-626: --auto flag parsing, config-set, Skill() chain to plan-phase

plan-phase.md:
- Line ~449-484: auto_advance step, --auto flag parsing, Skill() chain to execute-phase

execute-phase.md:
- Line ~33-34: --auto flag parsing at top of file
- Line ~192-193: AUTO_CHAIN config reading
- Line ~397: auto-advance reference
- Line ~416-430+: auto_advance step with Skill() chain

transition.md:
- Line ~381: `plan-phase [X+1] --auto")`
- Line ~393: `discuss-phase [X+1] --auto")`
- Line ~454-456: config-set for _auto_chain_active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy workflow files to project and remove --auto from discuss/plan/execute</name>
  <files>global/workflows/discuss-phase.md, global/workflows/plan-phase.md, global/workflows/execute-phase.md</files>
  <action>
IMPORTANT: The source workflow files live at `~/.claude/get-shit-done/workflows/`. The project repo stores templates in `global/workflows/`. We need to:

1. First check if `global/workflows/` exists in the project repo. If not, this plan needs to work directly on the installed files. Check the project structure:
   ```bash
   ls global/workflows/ 2>/dev/null
   ls global/ 2>/dev/null
   ```

2. If `global/workflows/` does not exist in the repo, the files to modify are the INSTALLED files at `~/.claude/get-shit-done/workflows/`. These are the actual source-of-truth files that get used. Modify them directly.

3. If `global/workflows/` DOES exist in the repo, modify both the repo copies AND the installed copies.

For each of the three files (discuss-phase.md, plan-phase.md, execute-phase.md):

**discuss-phase.md:**
- Find the `<step name="auto_advance">` section (around line 589)
- Remove the entire step including its closing `</step>` tag
- Replace with a brief note: `<!-- auto_advance removed in v2.0 -- use /gsd:drive for auto-chaining -->`
- Also remove any `_auto_chain_active` config-set calls at the top of the file if present

**plan-phase.md:**
- Find the auto_advance / offer_next conditional (around line 449-484)
- Remove the --auto branch. Keep the `offer_next` step content (the manual "next steps" output)
- Replace the auto_advance step with: `<!-- auto_advance removed in v2.0 -- use /gsd:drive for auto-chaining -->`
- Remove any `_auto_chain_active` config references

**execute-phase.md:**
- Find and remove --auto parsing near top (around line 33-34)
- Remove AUTO_CHAIN config reads (around line 192-193)
- Remove the auto_advance step (around line 416+)
- Replace with: `<!-- auto_advance removed in v2.0 -- use /gsd:drive for auto-chaining -->`
- Keep the manual completion output intact

For ALL files: ensure the step BEFORE the removed auto_advance still properly terminates. The workflow should end with "offer_next" or equivalent user-facing output, not a dangling reference to the removed step.

Do NOT modify any other logic in these files. Surgical removal only.
  </action>
  <verify>
    <automated>grep -c "auto_advance\|--auto\|_auto_chain_active" ~/.claude/get-shit-done/workflows/discuss-phase.md ~/.claude/get-shit-done/workflows/plan-phase.md ~/.claude/get-shit-done/workflows/execute-phase.md 2>/dev/null | grep -v ":0$" | wc -l | awk '{if ($1 == 0) print "PASS: no --auto references remain"; else print "FAIL: --auto references still present"}'</automated>
  </verify>
  <done>All three workflow files have --auto/auto_advance sections removed. No _auto_chain_active config references remain. Manual invocation still terminates at offer_next step with user-facing output.</done>
</task>

<task type="auto">
  <name>Task 2: Update transition.md to replace --auto chaining with /gsd:drive recommendation</name>
  <files>global/workflows/transition.md</files>
  <action>
Modify transition.md (at `~/.claude/get-shit-done/workflows/transition.md` and `global/workflows/transition.md` if it exists in repo):

1. Find the yolo mode auto-advance section that currently does:
   ```
   Skill(skill="gsd:plan-phase", args="[X+1] --auto")
   ```
   and
   ```
   Skill(skill="gsd:discuss-phase", args="[X+1] --auto")
   ```
   (around lines 381 and 393)

2. Replace these Skill() calls with a recommendation to use /gsd:drive:
   ```markdown
   **Auto-advance:** Phase transition complete. If running under /gsd:drive, the orchestrator will automatically continue to Phase {X+1}. If running manually, use `/gsd:drive` to continue.
   ```

3. Remove the `_auto_chain_active` config-set call (around line 456).

4. Keep ALL other transition logic intact: ROADMAP.md updates, PROJECT.md evolution, STATE.md advancement, the full transition flow. Only remove the --auto chaining at the end.

5. The yolo mode section should still detect whether more phases exist and report it, but should NOT auto-chain. It should end with a clear "phase complete" message.
  </action>
  <verify>
    <automated>grep -c "\-\-auto\|_auto_chain_active" ~/.claude/get-shit-done/workflows/transition.md 2>/dev/null | awk '{if ($1 == 0) print "PASS: no --auto references in transition.md"; else print "FAIL: "$1" --auto references remain"}'</automated>
  </verify>
  <done>transition.md no longer chains via --auto. Yolo mode reports phase completion and defers to /gsd:drive for continuation. All other transition logic (ROADMAP update, PROJECT evolution, STATE advancement) preserved intact.</done>
</task>

</tasks>

<verification>
- `grep -r "\-\-auto" ~/.claude/get-shit-done/workflows/{discuss,plan,execute}-phase.md` returns zero matches
- `grep -r "_auto_chain_active" ~/.claude/get-shit-done/workflows/` returns zero matches
- `grep -r "auto_advance" ~/.claude/get-shit-done/workflows/` returns zero matches (or only HTML comments)
- Each workflow file still has a clean termination point (offer_next or equivalent)
- transition.md still performs full phase completion logic
</verification>

<success_criteria>
All --auto flag handling, _auto_chain_active config usage, and auto_advance steps are removed from the four workflow files. Manual skill invocation still works (each skill terminates cleanly). transition.md yolo mode no longer auto-chains but references /gsd:drive.
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-orchestrator/01-02-SUMMARY.md`
</output>
