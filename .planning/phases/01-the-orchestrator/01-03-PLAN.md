---
phase: 01-the-orchestrator
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - bin/cli.sh
  - skills/init-gsd/SKILL.md
  - test-install.sh
autonomous: false
requirements: [ORCH-01, ORCH-04]

must_haves:
  truths:
    - "bin/cli.sh copies gsd-drive skill to ~/.claude/skills/gsd-drive/"
    - "test-install.sh verifies gsd-drive SKILL.md exists after install"
    - "init-gsd mentions /gsd:drive as the auto-chaining entry point"
    - "/gsd:drive is callable after running bin/cli.sh"
  artifacts:
    - path: "bin/cli.sh"
      provides: "Installation of gsd-drive skill alongside existing skills"
      contains: "gsd-drive"
    - path: "test-install.sh"
      provides: "Verification that gsd-drive is installed correctly"
      contains: "gsd-drive"
    - path: "skills/init-gsd/SKILL.md"
      provides: "Updated bootstrapper that references /gsd:drive"
      contains: "gsd:drive"
  key_links:
    - from: "bin/cli.sh"
      to: "skills/gsd-drive/SKILL.md"
      via: "cp/install command"
      pattern: "gsd-drive"
    - from: "test-install.sh"
      to: "~/.claude/skills/gsd-drive/SKILL.md"
      via: "file existence check"
      pattern: "gsd-drive"
---

<objective>
Wire the gsd-drive skill into the installation pipeline and verify the full integration works end-to-end.

Purpose: Without installation wiring, the skill exists in the repo but can't be used. This plan makes /gsd:drive installable and verifiable.
Output: Updated cli.sh, test-install.sh, and init-gsd SKILL.md. Human verification that /gsd:drive is callable.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-the-orchestrator/01-CONTEXT.md
@.planning/phases/01-the-orchestrator/01-01-SUMMARY.md
@.planning/phases/01-the-orchestrator/01-02-SUMMARY.md

@bin/cli.sh
@test-install.sh
@skills/init-gsd/SKILL.md

<interfaces>
<!-- From Plan 01: files created -->
skills/gsd-drive/SKILL.md       -- Main skill entry point
skills/gsd-drive/drive-workflow.md -- Detailed workflow logic

<!-- cli.sh pattern for skill installation (existing skills) -->
From bin/cli.sh:
- Skills are copied from $SCRIPT_DIR/skills/{name}/ to ~/.claude/skills/{name}/
- Uses mkdir -p for target directory
- Copies all files from skill directory
- Reports success/skip

<!-- test-install.sh pattern for verification -->
From test-install.sh:
- check() function verifies file existence
- Skills checked: init-gsd, codex-review, gsd-codex-verify
- Skill integrity: loops over skills/*/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gsd-drive to cli.sh installation and test-install.sh verification</name>
  <files>bin/cli.sh, test-install.sh, skills/init-gsd/SKILL.md</files>
  <action>
**bin/cli.sh changes:**

1. Read the current `bin/cli.sh` to find the skill installation section (look for where init-gsd, codex-review, gsd-codex-verify are copied).

2. Add gsd-drive skill installation in the same pattern. It should copy BOTH files:
   - `skills/gsd-drive/SKILL.md` -> `~/.claude/skills/gsd-drive/SKILL.md`
   - `skills/gsd-drive/drive-workflow.md` -> `~/.claude/skills/gsd-drive/drive-workflow.md`

   Follow the existing pattern exactly (mkdir -p, cp, ok/skip reporting). The existing code likely loops over `skills/*/` so if it does, gsd-drive will be picked up automatically -- verify this. If it uses explicit names, add gsd-drive.

3. Also check if cli.sh copies workflow files. Since we modified workflow files in Plan 02 (--auto removal), the `global/workflows/` copy step (if it exists) needs to include the updated files. If cli.sh already copies all of `global/workflows/`, this is handled automatically.

**test-install.sh changes:**

1. Add a check for `~/.claude/skills/gsd-drive/SKILL.md` in the skills verification section (alongside init-gsd, codex-review, gsd-codex-verify).

2. Add a check for `~/.claude/skills/gsd-drive/drive-workflow.md` (the workflow reference file must also be installed).

**skills/init-gsd/SKILL.md changes:**

1. Find where the skill lists available GSD commands or describes the workflow.

2. Add `/gsd:drive` as the primary auto-chaining entry point. Add a line like:
   - `/gsd:drive` -- Auto-drive the full GSD workflow (discuss -> plan -> execute -> verify -> advance)

3. Remove any references to `--auto` flag if present in init-gsd's documentation.

After all edits, run the installation to deploy:
```bash
cd /home/agent/projects/gsd-multi-model && bash bin/cli.sh --force
```

Then verify:
```bash
bash test-install.sh
```
  </action>
  <verify>
    <automated>bash /home/agent/projects/gsd-multi-model/test-install.sh 2>&1 | tail -5</automated>
  </verify>
  <done>cli.sh installs gsd-drive skill (SKILL.md + drive-workflow.md). test-install.sh verifies both files. init-gsd references /gsd:drive. `bash test-install.sh` passes with gsd-drive included.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete /gsd:drive integration</name>
  <files>none</files>
  <action>
Human verifies the complete /gsd:drive orchestrator integration:
- SKILL.md with argument parsing (bare, --phase N, --to N)
- drive-workflow.md with full state machine (discuss -> plan -> execute -> verify -> transition)
- --auto removed from all workflow files (discuss, plan, execute, transition)
- Installation wired through cli.sh and verified by test-install.sh

Verification steps:
1. Run `cat ~/.claude/skills/gsd-drive/SKILL.md | head -20` -- confirm frontmatter has name: gsd-drive
2. Run `cat ~/.claude/skills/gsd-drive/drive-workflow.md | head -30` -- confirm state machine sections exist
3. Run `grep -r "\-\-auto" ~/.claude/get-shit-done/workflows/{discuss,plan,execute}-phase.md ~/.claude/get-shit-done/workflows/transition.md` -- should return nothing
4. Run `bash test-install.sh` -- should pass with gsd-drive checks
5. In a Claude Code session on any project with .planning/, try `/gsd:drive` -- it should read STATE.md and report the next action
  </action>
  <verify>Human confirms all 5 verification steps pass</verify>
  <done>Human has approved the /gsd:drive integration. The skill is installed, callable, and the --auto removal is clean.</done>
</task>

</tasks>

<verification>
- `bash bin/cli.sh --force` installs gsd-drive skill without errors
- `bash test-install.sh` passes including gsd-drive checks
- `ls ~/.claude/skills/gsd-drive/` shows SKILL.md and drive-workflow.md
- `grep "gsd-drive\|gsd:drive" skills/init-gsd/SKILL.md` returns matches
- `/gsd:drive` is callable in Claude Code session
</verification>

<success_criteria>
The gsd-drive skill is installable via cli.sh, verified by test-install.sh, and callable as /gsd:drive in a Claude Code session. The human has confirmed the integration works.
</success_criteria>

<output>
After completion, create `.planning/phases/01-the-orchestrator/01-03-SUMMARY.md`
</output>
