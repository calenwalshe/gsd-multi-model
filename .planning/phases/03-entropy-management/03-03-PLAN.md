---
phase: 03-entropy-management
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - bin/test-entropy-sweep.sh
  - bin/test-check-doc-consistency.sh
autonomous: true
requirements:
  - ENTR-01
  - ENTR-02
  - ENTR-03
  - ENTR-04

must_haves:
  truths:
    - "Test suite validates sweep orchestrator dispatches correctly"
    - "Test suite validates doc consistency checker detects all three convention types"
    - "Test suite validates config defaults when entropy section is absent"
    - "Test suite validates architecture sweep collects full project files"
    - "Full test suite runs in under 30 seconds"
  artifacts:
    - path: "bin/test-entropy-sweep.sh"
      provides: "Integration tests for entropy-sweep.sh orchestrator"
      min_lines: 80
    - path: "bin/test-check-doc-consistency.sh"
      provides: "Tests for doc consistency checks"
      min_lines: 60
  key_links:
    - from: "bin/test-entropy-sweep.sh"
      to: "bin/entropy-sweep.sh"
      via: "test invocation with fixtures"
      pattern: "entropy-sweep\\.sh"
    - from: "bin/test-check-doc-consistency.sh"
      to: "bin/check-doc-consistency.sh"
      via: "test invocation with fixtures"
      pattern: "check-doc-consistency\\.sh"
---

<objective>
Build test suites for the entropy sweep orchestrator and doc consistency checker, then run full integration verification.

Purpose: Ensure all entropy management scripts are correct and robust via the test fixture pattern established in Phase 02. This plan validates the complete sweep pipeline end-to-end.

Output: `bin/test-entropy-sweep.sh`, `bin/test-check-doc-consistency.sh`
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entropy-management/03-RESEARCH.md
@.planning/phases/03-entropy-management/03-01-SUMMARY.md
@.planning/phases/03-entropy-management/03-02-SUMMARY.md

@bin/test-gate-check.sh
@bin/entropy-sweep.sh
@bin/check-doc-consistency.sh
@bin/check-stale-todos.sh

<interfaces>
<!-- From Plan 01: entropy-sweep.sh output -->
```json
{
  "sweep_type": "manual",
  "timestamp": "...",
  "schedule": "weekly",
  "checks": [
    {"name": "doc-consistency", "passed": bool, "findings": [...]},
    {"name": "architecture", "passed": bool, "findings": [...]},
    {"name": "stale-todos", "passed": bool, "findings": [...]}
  ],
  "summary": {"total_findings": N, "checks_run": N, "checks_passed": N}
}
```

<!-- From Plan 01: check-doc-consistency.sh output -->
```json
{
  "passed": true/false,
  "findings": [
    {"check": "debug-statements", "file": "...", "line": N, "text": "...", "severity": "warning"},
    {"check": "line-count", "file": "...", "lines": N, "limit": 200, "severity": "warning"},
    {"check": "missing-tests", "file": "...", "expected_test": "...", "severity": "info"}
  ]
}
```

<!-- Test fixture pattern from test-gate-check.sh -->
```bash
make_fixture() { ... }  # Creates temp git repo with config
pass() { echo "  PASS: $1"; PASS=$((PASS + 1)); }
fail() { echo "  FAIL: $1 -- $2"; FAIL=$((FAIL + 1)); }
cleanup() { rm -rf "$TMPDIR_ROOT"; }
trap cleanup EXIT
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build test-check-doc-consistency.sh</name>
  <files>bin/test-check-doc-consistency.sh</files>
  <action>
Create `bin/test-check-doc-consistency.sh` using the test-gate-check.sh fixture pattern (make_fixture, pass/fail helpers, cleanup trap, PASS/FAIL counters, exit code based on FAIL count).

Test cases:

1. **Detects console.log in production file**: Create fixture with `bin/app.sh` containing `console.log("test")`. Assert findings array has entry with check="debug-statements".

2. **Skips console.log in test files**: Create fixture with `bin/test-app.sh` containing `console.log("debug")`. Assert no findings for debug-statements check.

3. **Flags instruction file over 200 lines**: Create fixture with `skills/test-skill/SKILL.md` that is 210 lines. Assert findings array has entry with check="line-count".

4. **Passes instruction file under 200 lines**: Create fixture with a 150-line SKILL.md. Assert no line-count findings.

5. **Detects missing test file**: Create fixture with `bin/myapp.sh` but NO `bin/test-myapp.sh`. Assert findings has check="missing-tests".

6. **No finding when test file exists**: Create fixture with `bin/myapp.sh` AND `bin/test-myapp.sh`. Assert no missing-tests findings.

7. **Output is valid JSON**: Parse output with node -e, verify it has `passed` and `findings` keys.

8. **Passed is true when no violations**: Create clean fixture. Assert `passed === true`.

Each fixture needs an AGENTS.md file with the conventions section (copy from the real one, or create a minimal version with the checkable conventions).

Copy `check-doc-consistency.sh` into each fixture's `bin/` directory for self-contained testing.

Make executable.
  </action>
  <verify>
    <automated>bash bin/test-check-doc-consistency.sh</automated>
  </verify>
  <done>All doc consistency test cases pass. Tests cover debug statement detection with test file exclusion, line count checking, missing test detection, and valid JSON output.</done>
</task>

<task type="auto">
  <name>Task 2: Build test-entropy-sweep.sh</name>
  <files>bin/test-entropy-sweep.sh</files>
  <action>
Create `bin/test-entropy-sweep.sh` using the test-gate-check.sh fixture pattern.

Test cases:

1. **Runs all checks by default**: Create fixture with valid architecture config, an AGENTS.md, and a source file. Run `entropy-sweep.sh`. Assert output has `checks` array with entries for all three check types.

2. **--check flag runs single check**: Run `entropy-sweep.sh --check doc-consistency`. Assert output has exactly 1 check in the array.

3. **Config defaults when entropy section absent**: Create fixture with config.json that has NO entropy section. Run sweep. Assert it still runs (defaults to all enabled).

4. **Respects check disable flags**: Create fixture with config.json where `doc_consistency.enabled = false`. Run sweep. Assert doc-consistency check is skipped (not in checks array or marked skipped).

5. **Architecture sweep finds violations**: Create fixture with `.architecture.json` defining a boundary rule, then a file that violates it. Assert architecture check has findings.

6. **Architecture sweep passes on clean project**: Create fixture with valid architecture. Assert architecture check passes.

7. **Aggregates findings correctly**: Create fixture that triggers findings in doc-consistency. Assert `summary.total_findings` matches actual count.

8. **Handles missing check scripts gracefully**: Create fixture but DON'T copy check-stale-todos.sh. Assert sweep completes with stale-todos marked as skipped (not crash).

Each fixture needs: git repo, `.planning/config.json`, `.architecture.json`, `AGENTS.md`, and copies of the scripts being tested (`entropy-sweep.sh`, `check-doc-consistency.sh`, `validate-architecture.sh`, and optionally `check-stale-todos.sh`).

Make executable.
  </action>
  <verify>
    <automated>bash bin/test-entropy-sweep.sh</automated>
  </verify>
  <done>All entropy sweep test cases pass. Tests cover full dispatch, single check mode, config defaults, disable flags, architecture violations, finding aggregation, and graceful handling of missing scripts.</done>
</task>

</tasks>

<verification>
- `bash bin/test-entropy-sweep.sh` -- all tests pass
- `bash bin/test-check-doc-consistency.sh` -- all tests pass
- `bash bin/test-check-stale-todos.sh` -- all tests pass (from Plan 02)
- Full suite: `bash bin/test-entropy-sweep.sh && bash bin/test-check-doc-consistency.sh && bash bin/test-check-stale-todos.sh` completes in under 30 seconds
- `bash bin/entropy-sweep.sh` runs against real project and produces valid JSON
</verification>

<success_criteria>
- All three test suites pass (sweep, doc-consistency, stale-todos)
- Entropy sweep dispatches correctly to all check scripts
- Config defaults work when entropy section is absent
- Architecture sweep validates full project files via existing validator
- No test takes longer than 10 seconds individually
</success_criteria>

<output>
After completion, create `.planning/phases/03-entropy-management/03-03-SUMMARY.md`
</output>
