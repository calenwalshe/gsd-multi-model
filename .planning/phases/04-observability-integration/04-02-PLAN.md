---
phase: 04-observability-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - skills/gsd-debug/SKILL.md
  - skills/observe/SKILL.md
  - bin/test-install.sh
autonomous: true
requirements:
  - OBSV-02
  - OBSV-03

must_haves:
  truths:
    - "/gsd:debug loads observability config and invokes query-telemetry.sh to pull real error logs"
    - "Executor agents can query telemetry before/after task changes via observe skill"
    - "Both skills are no-ops when observability is unconfigured or disabled"
  artifacts:
    - path: "skills/gsd-debug/SKILL.md"
      provides: "/gsd:debug skill entry point for pulling error logs"
      min_lines: 30
    - path: "skills/observe/SKILL.md"
      provides: "Executor telemetry injection skill for before/after queries"
      min_lines: 25
  key_links:
    - from: "skills/gsd-debug/SKILL.md"
      to: "bin/query-telemetry.sh"
      via: "bash bin/query-telemetry.sh invocation"
      pattern: "query-telemetry"
    - from: "skills/observe/SKILL.md"
      to: "bin/query-telemetry.sh"
      via: "bash bin/query-telemetry.sh --json-only invocation"
      pattern: "query-telemetry"
---

<objective>
Create the /gsd:debug skill and executor observe skill that consume query-telemetry.sh.

Purpose: Give agents two entry points into telemetry -- /gsd:debug for interactive debugging sessions, and observe for automatic before/after telemetry during task execution. Both are opt-in and no-op when unconfigured.

Output: skills/gsd-debug/SKILL.md, skills/observe/SKILL.md, updated bin/test-install.sh
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-integration/04-RESEARCH.md
@.planning/phases/04-observability-integration/04-01-SUMMARY.md

@skills/gate-check/SKILL.md
@bin/test-install.sh

<interfaces>
<!-- From Plan 01: query-telemetry.sh interface -->
bin/query-telemetry.sh outputs:
- stdout: {"enabled":bool,"endpoints":{...},"results":[{"name":"...","type":"...","lines":[...],"error":null}],"summary":{"total":N,"success":N,"failed":N}}
- stderr: human-readable summary
- Flags: --endpoint NAME, --json-only, --project-root PATH, --health
- Exit: 0=ok, 1=findings, 2=config error

<!-- From gate-check skill: pattern to follow for skill structure -->
From skills/gate-check/SKILL.md:
- Frontmatter: name, description, argument-hint, allowed-tools
- Structured steps with bash commands
- Handles both success and failure paths
- References bin/ scripts directly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:debug and observe skills</name>
  <files>skills/gsd-debug/SKILL.md, skills/observe/SKILL.md</files>
  <action>
    Create skills/gsd-debug/SKILL.md:
    1. Frontmatter: name "gsd-debug", description about pulling real error logs from configured endpoints, argument-hint "[endpoint-name] [--last N]", allowed-tools: Read, Bash, Grep
    2. Purpose section: explains this pulls live telemetry data for debugging instead of relying on user-pasted logs
    3. Step 1 -- Check config: Read .planning/config.json, check if observability.enabled is true. If not configured or disabled, tell the user "Observability not configured. Add an observability section to .planning/config.json to enable. See bin/query-telemetry.sh header for config format." and stop.
    4. Step 2 -- Query endpoints: Run `bash bin/query-telemetry.sh` (or `bash bin/query-telemetry.sh --endpoint NAME` if user specified one). Capture both stdout (JSON) and stderr (human summary).
    5. Step 3 -- Present findings: Parse the JSON results. For each endpoint with results, format as a structured summary: endpoint name, type, number of lines, recent errors/warnings. For endpoints with errors (unreachable), note the issue. Highlight actionable items -- what errors are most recent, any patterns.
    6. Step 4 -- Suggest next steps: Based on findings, suggest debugging actions (e.g., "3 ERROR lines in app-logs in last hour -- check container restart", "Sentry shows 5 unresolved issues").
    7. Health check section: If user runs `/gsd:debug --health`, run `bash bin/query-telemetry.sh --health` to verify endpoint connectivity.

    Create skills/observe/SKILL.md:
    1. Frontmatter: name "observe", description about executor telemetry injection for before/after task comparison, argument-hint "", allowed-tools: Read, Bash
    2. Purpose section: explains this is loaded by executors during execute phase to capture telemetry snapshots before and after task changes
    3. Section "Before Task": If observability.enabled in config, run `bash bin/query-telemetry.sh --json-only` and store the JSON result. Summarize: "Current state: N errors in endpoint-A, M warnings in endpoint-B". If observability is not configured, state "Observability not configured -- skipping telemetry snapshot" and proceed normally.
    4. Section "After Task": If observability was active before, run `bash bin/query-telemetry.sh --json-only` again. Compare before/after: "Errors reduced from N to M in endpoint-A" or "New errors detected in endpoint-B: [first 3 lines]". If no change, state "Telemetry unchanged after task".
    5. Section "When to Skip": Skip telemetry if task is documentation-only, config-only, or planning file changes. Only query for code changes that could affect runtime behavior.
    6. Explicit note: This skill documents a protocol for executor agents to follow manually. It is NOT an automated hook. Executors invoke these steps at task boundaries.
  </action>
  <verify>
    <automated>test -f skills/gsd-debug/SKILL.md && test -f skills/observe/SKILL.md && grep -q "query-telemetry" skills/gsd-debug/SKILL.md && grep -q "query-telemetry" skills/observe/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>Both skills exist, reference query-telemetry.sh, handle unconfigured/disabled state, follow gate-check skill conventions</done>
</task>

<task type="auto">
  <name>Task 2: Update test-install.sh to verify new files</name>
  <files>bin/test-install.sh</files>
  <action>
    Add checks to bin/test-install.sh for the new Phase 04 artifacts:

    1. Read existing test-install.sh to understand the check pattern (ok/fail functions, file existence checks)
    2. Add file existence checks for:
       - bin/query-telemetry.sh (must exist and be executable)
       - bin/test-query-telemetry.sh (must exist)
       - skills/gsd-debug/SKILL.md (must exist)
       - skills/observe/SKILL.md (must exist)
    3. Add a config schema check: .planning/config.json must contain "observability" key
    4. Follow existing grouping pattern (group under a "Phase 04: Observability" comment section)

    Run `bash bin/test-install.sh` to verify it passes.
  </action>
  <verify>
    <automated>bash bin/test-install.sh</automated>
  </verify>
  <done>test-install.sh includes checks for all Phase 04 artifacts and passes</done>
</task>

</tasks>

<verification>
- `grep -q "gsd-debug" skills/gsd-debug/SKILL.md` confirms skill name
- `grep -q "observe" skills/observe/SKILL.md` confirms skill name
- `bash bin/test-install.sh` passes including new Phase 04 checks
- Both skills reference bin/query-telemetry.sh for actual queries
- Both skills handle unconfigured/disabled observability gracefully
</verification>

<success_criteria>
- /gsd:debug skill provides structured debugging workflow with live telemetry
- observe skill provides before/after telemetry protocol for executor agents
- Both are no-ops when observability is not configured
- test-install.sh validates all new artifacts exist
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-integration/04-02-SUMMARY.md`
</output>
