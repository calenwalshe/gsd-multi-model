---
phase: 02-task-splitting-routing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - /home/agent/.claude/agents/gsd-plan-checker.md
autonomous: true
requirements:
  - R4

must_haves:
  truths:
    - "gsd-plan-checker validates that all non-checkpoint tasks have executor and confidence attributes"
    - "gsd-plan-checker does NOT flag Phase 1 plans (backward compatibility)"
    - "Plan checker flags any checkpoint task with executor='codex' as an error"
  artifacts:
    - path: "/home/agent/.claude/agents/gsd-plan-checker.md"
      provides: "Executor attribute validation in plan checking"
      contains: "executor"
  key_links:
    - from: "/home/agent/.claude/agents/gsd-plan-checker.md"
      to: "/home/agent/.claude/agents/gsd-planner.md"
      via: "Checker validates what planner produces"
      pattern: "executor.*confidence"
---

<objective>
Update the gsd-plan-checker to validate executor and confidence attributes on task elements, completing the planning-side integration of task routing.

Purpose: Ensure the planner's routing output is structurally valid before plans reach execution. Checker catches missing attributes, mis-routed checkpoints, and other routing errors.

Output: Updated gsd-plan-checker.md with executor validation rules.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-splitting-routing/02-CONTEXT.md
@.planning/phases/02-task-splitting-routing/02-01-PLAN.md

<interfaces>
<!-- gsd-plan-checker.md: agent that verifies plans before execution -->
<!-- Has verification dimensions: goal coverage, task completeness, dependencies, context compliance -->
<!-- Target: add "Task Routing" as a new verification dimension -->

From /home/agent/.claude/agents/gsd-plan-checker.md:
- Verification dimensions checked: goal coverage, task completeness, dependency correctness, context compliance
- Returns structured PASS/ISSUES/FAIL result
- Runs in iteration loop with planner (max 3 rounds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add executor validation to gsd-plan-checker</name>
  <files>/home/agent/.claude/agents/gsd-plan-checker.md</files>
  <action>
Add a new verification dimension to gsd-plan-checker.md for task routing validation. This is a targeted addition — do NOT rewrite the entire file.

**Add a "Task Routing" verification dimension** alongside the existing dimensions (goal coverage, task completeness, etc.). The checker should verify:

1. **Attribute presence:** Every `<task>` element in the plan has both `executor` and `confidence` attributes.
   - Exception: Plans from phases before Phase 2 (phases 01-*) may lack these attributes. Do NOT flag Phase 1 plans as errors. Only validate plans from Phase 2 onward.
   - Detection: Check the plan's `phase:` frontmatter field. If it starts with `01-`, skip executor validation entirely.

2. **Valid values:** `executor` is one of: `claude`, `codex`. `confidence` is one of: `high`, `medium`, `low`. Any other value is an error.

3. **Checkpoint routing:** Any task with `type` starting with `checkpoint:` MUST have `executor="claude"`. If a checkpoint has `executor="codex"`, flag as ERROR with reason: "Checkpoint tasks require Claude interaction — cannot route to Codex."

4. **Confidence review hint:** If any task has `confidence="low"`, add an INFO-level note (not an error): "Task N has low routing confidence — user should review executor assignment."

**Severity levels for routing issues:**
- Missing executor/confidence on Phase 2+ plan: ISSUE (planner should fix)
- Invalid executor/confidence value: ISSUE
- Checkpoint routed to Codex: ERROR (blocks plan approval)
- Low confidence: INFO (advisory only)

**Where to add:** Insert the new dimension in the checker's verification checklist, after existing structural checks but before the final verdict. Use a subsection header like "### Task Routing Validation" or equivalent formatting consistent with the file.

**What NOT to change:**
- Do not modify existing verification dimensions
- Do not change the checker's return format
- Do not make executor validation block Phase 1 plans
  </action>
  <verify>
    <automated>grep -q "executor" /home/agent/.claude/agents/gsd-plan-checker.md && grep -q "confidence" /home/agent/.claude/agents/gsd-plan-checker.md && grep -q "checkpoint.*claude\|checkpoint.*codex\|checkpoint.*executor" /home/agent/.claude/agents/gsd-plan-checker.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  - gsd-plan-checker.md includes a Task Routing validation dimension
  - Validates executor and confidence presence on Phase 2+ plans
  - Skips validation for Phase 1 plans (backward compatibility)
  - Flags checkpoint tasks with executor="codex" as ERROR
  - Low confidence triggers INFO note, not error
  - No existing verification dimensions modified
  </done>
</task>

</tasks>

<verification>
- [ ] gsd-plan-checker.md contains executor validation rules
- [ ] Backward compatibility: Phase 1 plans not flagged for missing executor
- [ ] Checkpoint routing validation catches codex-routed checkpoints
- [ ] Low confidence is advisory (INFO), not blocking (ERROR)
- [ ] No existing checker functionality broken
</verification>

<success_criteria>
- Plan checker catches missing executor attributes on Phase 2+ plans
- Plan checker catches mis-routed checkpoint tasks
- Plan checker does not break validation of existing Phase 1 plans
- Low-confidence routing triggers user review hint without blocking
</success_criteria>

<output>
After completion, create `.planning/phases/02-task-splitting-routing/02-02-SUMMARY.md`
</output>
