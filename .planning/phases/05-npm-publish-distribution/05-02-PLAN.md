---
phase: 05-npm-publish-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - bin/cli.sh
autonomous: false
requirements:
  - DIST-01
  - DIST-02

must_haves:
  truths:
    - "npx gsd-multi-model installs skills only by default (safe default)"
    - "npx gsd-multi-model --all installs skills + codex config + rules + globals"
    - "Package is ready for npm publish (dry-run succeeds, all files present)"
  artifacts:
    - path: "bin/cli.sh"
      provides: "CLI entry point that handles default vs --all installation"
    - path: "package.json"
      provides: "npm package manifest with bin, files, metadata"
  key_links:
    - from: "npx gsd-multi-model"
      to: "bin/cli.sh"
      via: "npm bin field"
      pattern: '"gsd-multi-model".*cli.sh'
---

<objective>
Verify the full npx execution path works end-to-end and confirm publish readiness with a human checkpoint.

Purpose: DIST-01/DIST-02 -- confirm that `npx gsd-multi-model` and `npx gsd-multi-model --all` work correctly, and the package is ready for npm publish.
Output: Verified npx flow, publish-ready package.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-npm-publish-distribution/05-01-SUMMARY.md
@.planning/phases/05-npm-publish-distribution/05-RESEARCH.md

@bin/cli.sh
@package.json
@gsd-compat.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify npx execution path and fix any issues</name>
  <files>bin/cli.sh</files>
  <action>
1. Test the CLI locally by simulating the npx path:
   - Run `./bin/cli.sh` (no args) -- should install skills only (safe default)
   - Run `./bin/cli.sh --all` -- should install skills + codex config + rules + globals
   - Run `./bin/cli.sh --help` -- should display usage information

2. Test SCRIPT_DIR resolution: The cli.sh uses `BASH_SOURCE[0]` to find its package root. Verify this resolves correctly by:
   - Running from a different directory: `cd /tmp && /home/agent/projects/gsd-multi-model/bin/cli.sh --help`
   - This tests that SCRIPT_DIR finds skills/ relative to the script, not the CWD

3. Test via npm local install simulation:
   - Run `npm pack` to create the tarball
   - Install it to a temp location: `mkdir -p /tmp/gsd-test && cd /tmp/gsd-test && npm init -y && npm install /home/agent/projects/gsd-multi-model/gsd-multi-model-1.3.0.tgz`
   - Run the installed binary: `npx --prefix /tmp/gsd-test gsd-multi-model --help`
   - Verify SCRIPT_DIR resolves to the installed package (not the source repo)
   - Clean up: `rm -rf /tmp/gsd-test /home/agent/projects/gsd-multi-model/gsd-multi-model-*.tgz`

4. If any issues are found with SCRIPT_DIR resolution in the npx/installed context, fix them in bin/cli.sh. Common fix: resolve symlinks with `readlink -f` or the BASH_SOURCE approach documented in the research.

5. If --help flag is not implemented, add a simple usage block at the top of cli.sh:
```bash
if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then
  echo "gsd-multi-model -- Multi-model add-on for GSD"
  echo ""
  echo "Usage: npx gsd-multi-model [options]"
  echo "  (no args)    Install skills only (safe default)"
  echo "  --all        Install skills + codex config + rules + globals"
  echo "  --force      Overwrite existing files"
  echo "  --help       Show this help"
  exit 0
fi
```
  </action>
  <verify>
    <automated>cd /home/agent/projects/gsd-multi-model && bash -n bin/cli.sh && npm pack --dry-run 2>&1 | grep -c "cli.sh\|skills/\|global/\|rules/" && echo "PASS"</automated>
  </verify>
  <done>npx execution path works: default installs skills only, --all installs everything, --help shows usage. SCRIPT_DIR resolves correctly in both direct and installed contexts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of publish-ready package</name>
  <files>package.json, bin/cli.sh, gsd-compat.json</files>
  <action>Human verifies the complete npm package is ready for publish.</action>
  <what-built>Complete npm package ready for publish: version compat check, anti-duplication guards, fixed metadata, verified npx execution path.</what-built>
  <how-to-verify>
    1. Review the npm pack output: `cd ~/projects/gsd-multi-model && npm pack --dry-run`
    2. Verify all expected files are listed (bin/cli.sh, skills/, global/, rules/, gsd-compat.json)
    3. Verify NO planning artifacts or .git files are included
    4. Run `./bin/cli.sh --help` and confirm usage output
    5. Check version sync: `grep version package.json` and `grep addon_version gsd-compat.json` should match
    6. When ready to publish: `npm login` then `npm publish`
    7. After publish: `npx gsd-multi-model --help` from any directory to verify

    NOTE: Actual `npm publish` requires npm authentication (npm login) which is a manual step.
    The package.json repository URL may need updating to your actual GitHub org before publish.
  </how-to-verify>
  <verify>Human approval</verify>
  <done>Human has reviewed and approved the package for npm publish.</done>
  <resume-signal>Type "approved" to complete the phase, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `./bin/cli.sh` runs without error and installs skills
- `./bin/cli.sh --all` installs full set (skills + config + rules + globals)
- `npm pack --dry-run` shows correct file list
- Version compat check warns appropriately (tested in plan 01)
- Package metadata is complete and correct
</verification>

<success_criteria>
- npx gsd-multi-model installs skills only (safe default) -- DIST-01
- npx gsd-multi-model --all adds codex config, rules, globals -- DIST-01
- Package is publish-ready with correct bin, files, metadata -- DIST-02
- Human has verified and approved the package for publish
</success_criteria>

<output>
After completion, create `.planning/phases/05-npm-publish-distribution/05-02-SUMMARY.md`
</output>
